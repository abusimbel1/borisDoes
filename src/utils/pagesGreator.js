// import React, { useState } from "react";

// export function createPages(currentPage, portionSize=2, totalCount=10) {
//   let pages = new Array(portionSize);
//   let portionNumber = Math.ceil(currentPage/portionSize);
//   let portionCount = Math.ceil(totalCount/portionSize);
//   let leftPortionNumber = (portionNumber - 1) * portionSize + 1;
//   let rightPortionNumber = portionNumber * portionSize;

//   return pages.map((item,index) => (leftPortionNumber + index))


// }